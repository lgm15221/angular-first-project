import { EnlargeOnHover } from './enlarge-on-hover';

describe('EnlargeOnHover', () => {
  it('should create an instance', () => {
    const directive = new EnlargeOnHover();
    expect(directive).toBeTruthy();
  });
});
