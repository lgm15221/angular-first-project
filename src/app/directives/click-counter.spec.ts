import { ClickCounter } from './click-counter';

describe('ClickCounter', () => {
  it('should create an instance', () => {
    const directive = new ClickCounter();
    expect(directive).toBeTruthy();
  });
});
